主轴居中：justify-content：center;
侧轴居中：align-items：center;
隐藏溢出部分：overflow-hidden;
box-sizing:border-box  设置的边框和内边距的值是包含在width内的，内容区的实际宽度是width减去（border+padding）的值

Day1
transform
字面意思是：使…变形
属性的作用是对给定的元素旋转，缩放，平移或扭曲，通过修改元素的坐标空间实现。
使用方式：transform: rotate | scale | skew | translate |matrix;
所以当我们需要对元素做这些操作时，就需要使用 transform 属性。

translate
平移的意思，将元素按照坐标轴上下左右移动
使用方式：transform:translate(100px,50px);(x方向值,y方向值)元素较原先的位置，往右平移200px，往下平移50px，注意：translate属性需要在transform中才能使用，也属于修改元素的空间位置。

transition
翻译过来是过渡的意思，这个属性可以让元素的变化以动画的形式呈现，比如说过高度从100变到200，height:100px -> 200px ，没有其他属性的情况下，这个就是一瞬间的事，为了让页面交互友好些，希望高度的变化能有个过渡效果，那么 transition 就派上用场了。
transition 用来做动画效果非常的方便，但缺点也很明显，动画不支持循环，复杂的动画就难为它了，所以适用于一些简单的过渡效果。（transition：all 0.3s）
它有四个值：
transition-property：指定属性用于生成过渡动画，如宽、高、颜色等等，包括上面介绍的 transform ，只要这个属性的值是可以变化就行，且起始状态都是明确的。
transition-duration：动画执行时间
transition-timing-function：缓动函数， 内置了这些 ease | linear | ease-in | ease-out | ease-in-out ，也可以上 easings.net/ 挑选自己喜欢的效果
transition-delay：延迟执行的时间

以下这段css代码同时用上了我们刚介绍的三个属性，这段css的效果是，当鼠标移上元素的时候，元素在 200毫米内，匀速的往x轴和y轴放心移动了 100 像素
.translate:hover {
    transition: transform .2s ease;
    transform: translate(100px,100px);
}


Day2
动画-animation
过渡：实现两个状态间的变化过程
动画：实现多个状态间的变化过程，可控制（循环、播放、暂停）
定义动画：
@keyframe 动画名称 {                                                       
    from{}
    to{}
}
 @keyframe 动画名称 {

    0%{}
    10%{}
    。。。
    100%{}
}
使用动画：
animation：动画名称 时长s
animation的子属性有：
animation-delay：设置延时，即从元素加载完成之后到动画序列开始执行的这段时间
animation-direction：alternate反复播放。设置动画每次运行完后是反方向还是重新回到开始位置重复运行
animation-duration：持续时间。设置动画一个周期的时长。
animation-iteration-count：infinite无限次重复动画。设置动画重复次数
animation-name：指定由@keyframes描述的关键帧名称。
animation-play-state：允许暂停和恢复动画。（paused暂停）
animation-timing-function：linear 匀速。steps（）逐帧动画。设置动画速度，即通过建立加速曲线，设置动画在关键帧之间是如何变化的。
animation-fill-mode：指定动画执行前后如何为目标元素应用样式
复合写法：
animation: name duration timing-function delay iteration-count direction fill-mode;（用对应的属性值替换它们）


Day3
1、移动适配
vw：视口宽度的百分之一。vw 是相对单位，1vw 表示屏幕宽度的 1%
vh：视口高度的百分之一
rem：相对单位；1rem相当于1个html的大小，不同的设备有不同的根字体，通过媒体查询（检测视口的宽度）跟字号
rem单位尺寸：1、确定基准跟字号：查看设计稿宽度--》确定参考设备宽度（视口宽度）--》确定基准跟字号（1/10视口宽度） 2、rem单位尺寸：rem单位尺寸=px单位数值/基准跟字号
例：计算68px是多少个rem？（设计稿适配375px视口）
N*37.5 = 68、、、N=68/37.5




2、less
css预处理器 创建一个less文件输入out:./自动生成对应的css文件，在less文件中可以运算

（1）less变量：
@mycolor:red(定义变量)   .box{color：@mycolor（}使用变量）

（2）less-导入
作用：导入less公共样式文件
语法：导入@import "文件路径"
提示：如果是less文件可以省略后缀
@import './base.less'; === @import './base';
@import './common';
（3）less-导出
创建一个less文件输入out:./（./导出路径）
out: false（禁止导出）

Day4
响应式布局
flex-wrap：wrap (换行显示）